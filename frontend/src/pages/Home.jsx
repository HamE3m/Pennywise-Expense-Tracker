import React from 'react';
import ExpenseList from '../components/ExpenseList';

const Home = () => {
    return (
        <div>
            <h1>Expense Tracker</h1>
            <ExpenseList />
        </div>
    );
};

export default Home;